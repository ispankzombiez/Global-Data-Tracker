This folder contains the static GitHub Pages dashboard.

Pages deployment copies this folder to the site root and injects processed data from `data/processed` under `data/processed/` in the published artifact.
